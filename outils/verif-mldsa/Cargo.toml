[package]
name = "verif-mldsa"
version = "0.1.0"
edition = "2021"

# Projet jetable, dont le seul but est de decouvrir l'API reelle du crate
# `ml-dsa` et de verifier que les tailles annoncees par Q21 sont les bonnes.
[dependencies]
ml-dsa = "0.1.1"
rand_core = "0.10"
